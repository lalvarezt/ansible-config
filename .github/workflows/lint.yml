---
name: Ansible Lint

"on":
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Ansible Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v4

      - name: Install Ansible collections
        run: |
          uvx --from ansible-core ansible-galaxy collection install -r requirements.yml

      - name: Run yamllint
        run: |
          uvx yamllint -d relaxed ansible/

      - name: Run ansible-lint
        working-directory: ansible
        run: |
          uvx ansible-lint playbooks/*.yml

      - name: Check Ansible syntax
        working-directory: ansible
        run: |
          uvx --from ansible-core ansible-playbook playbooks/*.yml --syntax-check -i inventory/hosts.yml
