---
# Git and version control tools installation

- name: Install Git Tools
  hosts: all
  gather_facts: true
  tags: [git]

  vars:
    # Normalize modules input
    _modules_raw: "{{ (modules | from_yaml) if modules is defined and modules is string else (modules | default([])) }}"
    modules_list: "{{ [_modules_raw] if _modules_raw is string else _modules_raw }}"

  tasks:
    - name: Install git tools
      include_role:
        name: package_installer
      vars:
        package_info: "{{ packages.git_tools[item] }}"
        pkg_name: "{{ item }}"
      loop: "{{ packages.git_tools.keys() | list }}"
      when: >
        modules_list | length == 0 or
        (
          (packages.git_tools[item].tags | default([]))
          + (['uv'] if packages.git_tools[item].uv is defined else [])
          + (['brew'] if packages.git_tools[item].brew is defined else [])
          + (['go'] if packages.git_tools[item].go is defined else [])
          + (['cargo'] if packages.git_tools[item].cargo is defined else [])
          + (['native'] if packages.git_tools[item].native is defined else [])
        ) | intersect(modules_list) | length > 0
