---
# Language package managers (uv, go, rustup/cargo)

- name: Install Language Package Managers
  hosts: all
  gather_facts: true

  roles:
    - role: lang_managers
      tags: [development, core]
