---
# Fedora-specific system prerequisites

- name: Install system prerequisites for Fedora
  ansible.builtin.dnf:
    name: "{{ system_prereqs }}"
    state: present
  become: true
  when: system_prereqs is defined
