---
# Ubuntu-specific system prerequisites

- name: Update apt cache
  apt:
    update_cache: true
    cache_valid_time: 3600
  become: true

- name: Install system prerequisites for Ubuntu
  apt:
    name: "{{ system_prereqs }}"
    state: present
  become: true
  when: system_prereqs is defined
